import { notFound } from "next/navigation"

/**
 * Página de políticas desactivada temporalmente a petición del usuario.
 * El contenido anterior está respaldado en `app/politicas/page.tsx.bak`
 * para ser reactivado o actualizado cuando se suministre la nueva información.
 */
export default function PoliciesPage() {
    notFound()
}
